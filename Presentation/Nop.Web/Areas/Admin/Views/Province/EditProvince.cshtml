@model ProvinceModel

@{
    //page title
    ViewBag.PageTitle = T("Admin.Catalog.Province.EditDetails").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Provinces");
}

<form asp-controller="Province" asp-action="EditProvince" method="post" id="Province-form">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.Catalog.Province.EditDetails") - @Model.Name
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="ListProvince">@T("Admin.Catalog.BackToList")</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.SaveContinue")
            </button>
            <span id="Province-delete" class="btn bg-red">
                <i class="fa fa-trash-o"></i>
                @T("Admin.Common.Delete")
            </span>
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdate", Model)
</form>
<nop-delete-confirmation asp-action="DeleteProvince" asp-model-id="@Model.Id" asp-button-id="Province-delete" />